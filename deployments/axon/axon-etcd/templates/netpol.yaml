apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: axon-etcd
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    matchLabels:
      app: axon-etcd
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: axon
      ports:
        - port: 2379
          protocol: TCP
