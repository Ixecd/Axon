apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: axon-postgres
  namespace: {{ .Release.Namespace }}
  labels:
    app: axon-postgres
spec:
  serviceName: axon-postgres
  replicas: 1
  selector:
    matchLabels:
      app: axon-postgres
  template:
    metadata:
      labels:
        app: axon-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: axon-postgres-auth
                  key: password
            - name: POSTGRES_DB
              value: axon
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "user", "-d", "axon"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "user", "-d", "axon"]
            initialDelaySeconds: 15
            periodSeconds: 10
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: {{ .Values.storage }}
